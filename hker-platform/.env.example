# 複製這個檔案為 .env.local（本機開發）並填入你自己的金鑰。
# .env.local 不可以上傳到 GitHub 或任何公開的地方，也不可以貼在聊天訊息、文件檔案裡。
# 在 Vercel 部署時，這些值改成在 Vercel 專案 Settings → Environment Variables 裡設定。

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

NEWS_API_KEY=
GEMINI_API_KEY=
